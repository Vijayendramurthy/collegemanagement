# Example environment variables for backend (do NOT commit real secrets)
PORT=5000
MONGO_URI=your-mongodb-uri-here
# Optional: SMTP credentials for sending real emails (Gmail app password recommended)
# If you provide these in backend/.env, OTPs will be emailed. Do NOT commit real credentials.
EMAIL_USER=your-email@example.com
EMAIL_PASS=your-email-password-or-app-password
